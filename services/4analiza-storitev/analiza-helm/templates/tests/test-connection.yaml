apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "analiza-helm.fullname" . }}-test-connection"
  labels:
    {{- include "analiza-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "analiza-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
