apiVersion: v1
kind: Service
metadata:
  name: {{ include "stroski-helm.fullname" . }}
  labels:
    {{- include "stroski-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stroski-helm.selectorLabels" . | nindent 4 }}
