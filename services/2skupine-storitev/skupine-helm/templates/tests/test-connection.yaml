apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skupine-helm.fullname" . }}-test-connection"
  labels:
    {{- include "skupine-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skupine-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
