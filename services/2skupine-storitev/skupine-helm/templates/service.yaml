apiVersion: v1
kind: Service
metadata:
  name: {{ include "skupine-helm.fullname" . }}
  labels:
    {{- include "skupine-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skupine-helm.selectorLabels" . | nindent 4 }}
